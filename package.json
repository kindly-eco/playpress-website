{
  "private": true,
  "name": "playpress-website",
  "version": "0.0.0",
  "description": "The website of Playpress",
  "scripts": {
    "build": "postcss src/*.css --dir assets",
    "start:theme": "theme watch --allow-live --notify='http://localhost:35729/changed'",
    "start:css": "postcss src/*.css --dir assets --watch",
    "start:live": "node live-reload.js",
    "start": "npm-run-all --parallel start:**",
    "check:css": "stylelint '**/*.css' --ignore-path .gitignore",
    "check:format": "prettier '**/*.{css,html,js,json,md,yml}' --check --ignore-path .gitignore",
    "check:js": "eslint '**/*.js' --ignore-path .gitignore",
    "check": "npm-run-all check:*",
    "test": "npm run check",
    "format": "prettier '**/*.{css,html,js,json,md,yml}' --write --ignore-path .gitignore"
  },
  "devDependencies": {
    "@signal-noise/stylelint-scales": "^2.0.0",
    "autoprefixer": "^9.8.6",
    "cssnano": "^5.0.2",
    "eslint": "^7.16.0",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.2.1",
    "postcss-cli": "^8.3.1",
    "postcss-import": "^14.0.1",
    "postcss-logical": "^4.0.2",
    "postcss-nesting": "^7.0.1",
    "prettier": "2.2.1",
    "stylelint": "^13.8.0",
    "stylelint-config-recommended": "^3.0.0",
    "stylelint-order": "^4.1.0",
    "stylelint-use-logical": "^1.1.0",
    "stylelint-use-nesting": "^3.0.0",
    "tiny-lr": "^2.0.0"
  }
}
